<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.inphynous.futuresupport8x" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/future_supportt"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/future_supportt_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".update_acc_detail"
            android:parentActivityName=".AccountActivity" />
        <activity android:name=".admin" />
        <activity android:name=".Add_Data_Activity" />
        <activity
            android:name=".unique"
            android:parentActivityName=".LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity android:name=".Detailp" />
        <activity
            android:name=".AccountActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".signupactivity"
            android:parentActivityName=".LoginActivity" />
        <activity android:name=".signup" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".HomeActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>